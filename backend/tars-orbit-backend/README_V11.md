# TARS ORBIT Backend V11

This version expands the course catalog, repairs `&amp;` entities, and ensures every course has at least five modules and daily assignments during `npm run db:init`.

Run:
1. `npm install`
2. Configure `.env` from `.env.example`
3. `npm run db:init`
4. `npm run dev`

The existing admin routes/panel are unchanged.
